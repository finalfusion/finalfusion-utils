language: rust
sudo: required
dist: trusty
addons:
  apt:
    packages:
      - libopenblas-dev
      - gfortran
rust:
  - 1.32.0
  - stable
install:
  - rustup component add clippy
  - rustup component add rustfmt
script: ci/script.sh